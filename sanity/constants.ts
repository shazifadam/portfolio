// Fixed document _ids for singleton documents. Kept dependency-free so app
// code (server components, lib/) can import them without pulling the
// `sanity` Studio package into the Next.js bundle.
export const LINKS_PAGE_ID = "linksPage";
